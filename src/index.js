import React from 'react';
import { render } from 'react-dom';
import App from './containers/App';

// index.js loads App as the top level component.

render (
  <App />,
  document.getElementById('root')
);
